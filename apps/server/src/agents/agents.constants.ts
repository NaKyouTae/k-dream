/**
 * 에이전트 계정을 만들 때 넣는 초기 비밀번호.
 *
 * 관리자가 계정을 만들어 현지 에이전트에게 전달하는 구조라, 매번 다른
 * 비밀번호를 정해 따로 알려주는 절차가 번거로워 고정값을 쓴다.
 * 값이 공개되어 있는 셈이므로 전달 후 변경을 안내해야 한다.
 */
export const DEFAULT_AGENT_PASSWORD = "123456789a";
